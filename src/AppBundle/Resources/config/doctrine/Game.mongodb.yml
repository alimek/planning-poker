AppBundle\Document\Game:
    repositoryClass: AppBundle\Repositories\GameRepository
    collection: game
    fields:
        id:
            id:  true
        name:
            type: string
        players:
            type: collection
    embedMany:
        tasks:
            targetDocument: Task
    embedOne:
        task:
            targetDocument: Task
